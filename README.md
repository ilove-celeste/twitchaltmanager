# Twitch Alt Manager 🎮

Быстрое переключение между аккаунтами Twitch — без ввода паролей и кодов из почты.

## Как это работает

Расширение сохраняет **cookies сессии** каждого аккаунта локально (зашифровано через AES-256-GCM).  
При переключении — просто подменяет куки и перезагружает вкладки Twitch.  
Никаких паролей не хранится. Данные не покидают браузер.

---

## Установка (ungoogled-chromium)

1. Распакуй папку `twitch-alt-manager` в удобное место (например, `C:\extensions\twitch-alt-manager`)
2. Открой `chrome://extensions/`
3. Включи **«Режим разработчика»** (Developer mode) — переключатель вверху справа
4. Нажми **«Загрузить распакованное расширение»** (Load unpacked)
5. Выбери папку `twitch-alt-manager`
6. Расширение появится в панели

> **Важно для ungoogled-chromium:** если при установке появляется ошибка о подписи — это нормально для режима разработчика. Просто подтверди установку.

---

## Первое использование

### Сохранить аккаунт

1. Войди в нужный аккаунт Twitch в браузере как обычно
2. Открой попап расширения (иконка в панели)
3. Нажми **«+ Сохранить»**
4. Введи имя (или оставь как есть — подтянется из куков)
5. Нажми **«Сохранить»**
6. Повтори для каждого аккаунта

### Переключиться

- Просто **кликни по нужному аккаунту** в попапе
- Страницы Twitch перезагрузятся — ты уже под новым аккаунтом

### Обновить сессию

Если сессия протухла (Twitch попросил снова войти):
1. Войди вручную в браузере
2. В попапе нажми иконку 🔄 рядом с аккаунтом
3. Расширение перезапишет куки свежими

---

## Структура файлов

```
twitch-alt-manager/
├── manifest.json      # MV3 конфиг расширения
├── background.js      # Service Worker: управление куками, шифрование, хранение
├── popup.html         # UI попапа
├── popup.js           # Логика попапа
├── content.js         # Минимальный content script для Twitch страниц
└── icons/             # Иконки расширения
    ├── icon16.png
    ├── icon48.png
    └── icon128.png
```

---

## FAQ

**Почему нужно сохранять каждый аккаунт вручную один раз?**  
Расширение не знает пароли — оно работает с cookies. Нужно войти в аккаунт хотя бы один раз, чтобы Twitch выдал куки сессии.

**Безопасно ли это?**  
Куки хранятся только локально в `chrome.storage.local` в зашифрованном виде (AES-256-GCM). Ключ шифрования генерируется браузером и тоже хранится локально. Никакие данные никуда не отправляются.

**Что если Twitch добавил 2FA?**  
2FA проверяется при **входе**, а не при каждом запросе. Если ты уже вошёл и сохранил куки — переключение не потребует 2FA.

**Куки протухают?**  
Расширение продлевает срок action-кук при сохранении (+30 дней). Но если Twitch принудительно инвалидирует сессию — придётся войти заново и обновить сессию кнопкой 🔄.

---

## Изменения в v1.2.0

Исправлены 16 найденных проблем в коде:

| # | Файл | Проблема | Исправление |
|---|---|---|---|
| 1 | background.js | `getAllTwitchCookies` не захватывал cookies с доменом `.twitch.tv` | Запрос по 6 доменам + дедупликация |
| 2 | background.js | `setCookies` молча игнорировал ошибки | Логирование + отчёт succeeded/failed |
| 3 | background.js | Имя аккаунта не парсилось из JSON `twilight-user` | `extractUsernameFromTwilightUser()` с JSON.parse |
| 4 | content.js | Удаление IndexedDB могло зависнуть при занятой базе | `deleteDatabaseSafe()` с onblocked/onerror/таймаутом |
| 5 | popup.js | `escHtml` не экранировал `'` | Добавлена замена на `&#39;` (и заменено на DOM API) |
| 6 | manifest.json / background.js | `periodInMinutes: 0.4` — ниже минимума Chrome (1 мин) | Изменено на `1` |
| 7 | background.js | Не проверялась установка `auth-token` перед перезагрузкой вкладок | Проверка `setReport.succeeded` — при неудаче переключение прерывается |
| 8 | background.js | `pendingLocalStorage` не чистился при удалении аккаунта | Добавлена очистка в `deleteAccount` |
| 9 | popup.js | Сравнение по `label`, а не по реальному cookie-логину | `state.currentUser` теперь всегда из cookies |
| 10 | popup.js | `innerHTML` с непроверенными данными | Полностью переписано на `createElement`/`textContent` |
| 11 | content.js | `chrome.storage.local.get` без проверки `lastError` | Добавлена проверка `chrome.runtime.lastError` |
| 12 | background.js | Нет проверки наличия вкладки Twitch | Явная проверка + понятная ошибка |
| 13 | background.js | Задержка 150мс не гарантировала завершение очистки | `sendMessage` с ожиданием ack + таймаут |
| 14 | popup.js | Попап закрывался до полного завершения переключения | Закрытие только после `loadState()` |
| 15 | background.js | `decrypt`/`JSON.parse` без раздельной обработки ошибок | Отдельные try/catch с понятными сообщениями |
| 16 | content.js / background.js | `pendingLocalStorage` мог примениться к чужому аккаунту | Привязка к `accountId`, проверка перед восстановлением |

**Новая структура `pendingLocalStorage`:**
```js
{
  accountId: "acc_1234567890",
  data: { "twilight-user": "...", ... },
  createdAt: 1234567890
}
```

## Изменения в v1.3.0

По каждому пункту разбор: реальная проблема или нет, и что сделано.

| # | Реальна? | Файл | Что исправлено |
|---|---|---|---|
| 1 | Да, но не «обфускация» | background.js | Ключ шифрования теперь хранится как **non-extractable CryptoKey в IndexedDB**, а не сырые байты в `chrome.storage.local`. Байты ключа нельзя прочитать даже кодом расширения — только использовать через `encrypt`/`decrypt`. Старый ключ автоматически мигрирует (сохранённые аккаунты не ломаются) |
| 2 | Да | background.js, content.js | `sessionStorage` теперь захватывается при сохранении и восстанавливается при переключении наравне с `localStorage` |
| 3 | Да | background.js | Проверяются все критичные cookies (`auth-token`, `twilight-user`, `login`, `persistent`, `api_token`, `server_session_id`), но только те, что реально были в сохранённом наборе — чтобы не было ложных ошибок |
| 4 | Да, это был баг | background.js | `computeExpirationDate()` — истёкшие cookies больше не продлеваются искусственно; сессионные остаются сессионными; продлеваются только валидные с оставшимся сроком < 30 дней |
| 5 | **Нет, не баг** | background.js | `cookieUrl` уже делал ровно то, что требовалось. Комментарий уточнён |
| 6 | Да | background.js | `captureStorageFromActiveTab` требует строго активную вкладку, без фолбэка на «любую» |
| 7 | Да | background.js | `extractUsernameFromTwilightUser` возвращает `null` при неудаче `JSON.parse`, а не сырую строку |
| 8 | Да | background.js | Убран фолбэк `Account ${Date.now()}` — при неудаче автоопределения имени выбрасывается ошибка с просьбой указать имя вручную |
| 9 | Да | popup.js | `escHtml` удалена как мёртвый код |
| 10 | **Нет, CSS уже был не inline** | popup.html/css | CSS и так был собран в `<style>`, а не разбросан. Тем не менее вынесен в отдельный `popup.css` для порядка |
| 11 | Да | popup.js | Задержка перед закрытием попапа увеличена с 800 до 2000мс |
| 12 | Да | popup.js | `Enter` в поле имени теперь делает `preventDefault()` и проверяет `!btnConfirm.disabled` |
| 13 | Частично | manifest.json, background.js | Разрешение `alarms` и keep-alive убраны: SW и так просыпается по событиям, длительность операций — секунды |

### Модель угроз (шифрование)

Ключ теперь **non-extractable CryptoKey** в IndexedDB — это существенно надёжнее хранения сырых байт, но не абсолютная защита:

- ✅ Защищает от копирования профиля браузера на другую машину и попытки вычитать ключ напрямую из файлов
- ✅ Защищает от кода вне расширения (другие расширения, сайты)
- ❌ **Не защищает** от кода, который выполняется в контексте самого расширения (например, через remote debugging с полным доступом к браузеру) — такой код может вызвать `decrypt()` через тот же API, что и сама эта функция
- ❌ Не защищает, если скомпрометирован сам браузер/ОС на уровне root/administrator

Это фундаментальное ограничение любого локального хранилища расширений без внешнего секрета (мастер-пароля). Полноценная защита потребовала бы пароля, который пользователь вводит при каждом открытии попапа — что противоречит цели «переключение в один клик».

## Вдохновение

Идея взята из мода [In-Game Account Switcher](https://modrinth.com/mod/in-game-account-switcher) для Minecraft — мгновенное переключение аккаунтов без перезапуска.
