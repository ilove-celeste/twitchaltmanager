{
  "name": "twitch-alt-manager",
  "version": "1.4.0",
  "private": true,
  "description": "Tests for Twitch Alt Manager Chromium MV3 extension",
  "scripts": {
    "test": "jest --runInBand",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e"
  },
  "devDependencies": {
    "jest": "^30.1.3",
    "@playwright/test": "^1.55.0"
  }
}
